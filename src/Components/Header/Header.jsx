import Navbar from "./Navbar";

const Header = () => {
    return (
        <div>
            <nav>
                <Navbar></Navbar>
            </nav>
        </div>
    );
};

export default Header;